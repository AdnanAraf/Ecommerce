import React from "react";
import { useEffect } from "react";
import { useState } from "react";

const ReviewDetailsCard = ({ item }) => {
  console.log(item);

  return (
    <div>
      <div>
        {/* <div>
          {card.map((item) => {
            return <h1>{item.review}</h1>;
          })}
        </div> */}
      </div>
    </div>
  );
};

export default ReviewDetailsCard;
